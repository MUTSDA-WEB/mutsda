PORT=SERVER_PORT_NUMBER

# for local db 
DATABASE_URL="postgresql://USERNAME:PASSWORD@localhost:PORT/DBNAME?schema=public"

# for signing session tokens 
JWT_SECRET=YOUR_SECRET_KEY

# SOCKET.IO
SOCKET_PORT=YOUR_SOCKETIO_PORT

# REDIS 
REDIS_URL=REDIS_URL

CLIENT_URL="CLIENT URL WHERE MESSAGES ARE COMING FROM"

# cloudinary - for image hosting in the cloud 
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Email Configuration - for sending welcome emails to board members
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password